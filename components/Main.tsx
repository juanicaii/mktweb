import styles from "../styles/Home.module.css";

const Main = () => {
  return (
    <div
      style={{ backgroundImage: "url('./fondo.jpeg')" }}
      className={`${styles.main}`}
    ></div>
  );
};

export default Main;
